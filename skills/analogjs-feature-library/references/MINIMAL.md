# Minimal Library (No Options)

Reference for the minimal library generated by `@analog-tools/generator:library` with no additional options.

```
libs/my-feature/
├── src/
│   ├── index.ts                           # Public API barrel
│   ├── test-setup.ts                      # Vitest configuration
│   └── lib/
│       ├── components/                    # Angular components
│       │   └── my-feature.component.ts
│       └── services/                      # Shared services
│           └── my-feature.service.ts
├── eslint.config.cjs
├── package.json
├── project.json
├── README.md
├── tsconfig.json
├── tsconfig.lib.json
├── tsconfig.spec.json
└── vite.config.mts
```

Use for: UI components, shared utilities, domain logic (no pages, no backend).
